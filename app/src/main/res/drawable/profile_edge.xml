<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 배경색 (투명하게 하고 싶으면 @null) -->
    <solid android:color="@null"/>

    <!-- 테두리 (두께, 색깔) -->
    <stroke android:width="5sp" android:color="@color/colorPrimary"/>

    <padding android:left="1sp" android:right="1sp" android:bottom="1sp" android:top="1sp"/>

    <corners android:radius="1000sp"/>

</shape>